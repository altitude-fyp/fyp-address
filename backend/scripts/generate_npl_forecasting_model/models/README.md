this directory contains the trained models for the npl forecasting
    - models.sav 

model.sav contains a dictionary
    key = model name
    value = trained model object

model.sav contains 5 top performing models